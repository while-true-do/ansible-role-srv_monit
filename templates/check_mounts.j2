{% for mount in wtd_srv_monit_conf_check_mounts.mounts %}
check device {{ mount.name }} with path {{ mount.mount }}
    if space usage > 85% for 15 cycles then alert
    if space usage > 95% for 5 cycles then alert
    if inode usage > 95% for 5 cycles then alert

{% endfor %}
